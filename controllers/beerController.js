const db = require("../models");

module.exports = {
    // create a beer input in the DB

    // create a brewery input in the DB

    // pull up beer matches based on user query

    // pull up brewery matches based on user query
}